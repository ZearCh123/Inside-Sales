import { ModulePlaceholder } from "@/components/module-placeholder";
import { Calendar } from "lucide-react";

export default function Page() {
  return <ModulePlaceholder title="Calendar" icon={Calendar} phase={5} />;
}
